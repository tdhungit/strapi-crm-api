import { factories } from '@strapi/strapi';

export default factories.createCoreController(
  'api::shipping-method.shipping-method',
  ({ strapi }) => ({
    // @TODO: Implement the controller methods here
  }),
);
